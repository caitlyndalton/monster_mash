@campaigns.map do |campaign|
  {
    id: campaign.id,
    name: campaign.name,
    description: campaign.description,
    creatures: campaign.creatures,
    items: campaign.items,
    npcs: campaign.npcs,
  }
end